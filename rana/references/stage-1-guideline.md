# Stage 1 Guideline — Basic + Core 输出

## 前置条件

- 用户已触发 rana skill
- 已完成启动自检（blockStreaming 配置检查）
- 文件预处理已完成（如有文件输入）

---

## 执行步骤

### Step 1: 知识库检索（无感触发）

在提示用户提供材料之前，先检索在线知识库获取相关上下文。

**知识库地址**：`http://10.109.65.184:3000/zh-context/`

**R1：扫描知识库产品线列表**

使用 `curl` 请求知识站点首页，提取所有产品线目录名。若知识站点不可访问，跳过检索步骤。

**R2：判断匹配产品线**

根据用户已提供的任何信息（需求名称、PRD 关键词、产品名称）：
- 判断最可能匹配的产品线目录
- 一次只匹配一个产品线（最高置信度优先）
- 若用户尚未提供任何信息，跳过知识库检索

**R3（匹配成功）：按需读入知识内容**

1. 访问该产品线页面，列出所有文档标题
2. 优先读入 `overview` 概览文档（如存在）
3. 根据需求关键词，再选 1-2 个最相关的专项文档读入

**注意**：知识库内容作为分析上下文，不直接向用户输出，不在分析文档中重复抄写。

---

### Step 2: 接收输入

在启动分析前，提示用户：

> 请提供以下材料（有什么提供什么，不必齐全）：
> 1. PRD 文本（直接粘贴，或上传文件）
> 2. PRD 文件（支持 .pdf，直接上传即可）
> 3. 界面截图（如有）
> 4. 其他背景说明（业务背景、迭代目标、版本计划等）

**文件输入处理**：

- **Figma 输入**：暂不支持 Figma 链接或 .fig 文件
- **PDF 文件**：直接将 `.pdf` 文件上传给模型即可
- **Excel 文件**：暂不支持，引导用户导出为 PDF

---

### Step 3: 映射材料到模板章节

根据材料充分度，填充模板一~四章节：

| 材料类型 | 可填充的章节 |
|---------|-------------|
| PRD 文本 | 1.1 文档基础信息；2.1 需求来源、2.3 影响范围；3.1 现状描述等 |
| 界面截图 | 3.1 现状描述（补充视觉现象）等 |
| 业务背景说明 | 1.1 文档基础信息；2.3 影响范围等 |
| 用户数据/反馈 | 4.1 核心用户画像等 |
| 口头说明 | 作为后续对话引用 |

---

### Step 4: 创建 final-analysis.md（初始化）

读取 `assets/analysis-template.md`，填充可完成的章节，无法完成的标记 `[缺失]`。

**注意**：本阶段仅输出一~四章节（Basic + Core）。五~九章在 Stage 3 输出。

---

## 门禁检查

### 逻辑合理性诊断

在检查 P0 缺口之前，执行内部逻辑自检：

1. **方案-痛点匹配检查**：PM 提出的方案是否真能解决核心问题？
2. **目标冲突检查**：业务目标与用户体验目标是否严重冲突？
3. **迁移成本自检**：对比用户当前解决此问题的"旧方案"，本次"新方案"是否带来过高迁移成本？

发现逻辑疑点 → 列入 P0 探讨项，在缺口讨论环节提出质疑。

### P0 缺口检查

检查以下 P0 缺口（见 `references/p0-gates.md`）：

**Basic 阶段 P0**：
- 1.1 需求名称、业务线
- 2.1 需求来源（至少一项）

**Core 阶段 P0**：
- 3.1 现状描述（至少一个核心现象）
- 4.1 核心用户画像（基础属性 + 痛点）
- 4.3 核心使用场景（5要素：用户+场景+任务+障碍+目标）

---

## 输出：四段式独立消息

**消息块 1（引导语）**：
> 正在为你展示 Basic + Core 阶段完整分析文档

**消息块 2（报告本体）**：
- 静默读取 final-analysis.md 已完成部分（一~四章节）
- 逐字全文输出，不省略、不截断、不使用代码块包裹
- 不输出五~九章模板内容（此阶段未填充）

**消息块 3（缺口清单）**：
- 检查 P0 缺口
- 无缺口：`✓ 无 P0 缺口阻塞`
- 有缺口：呈现缺口表格 + 进入缺口讨论环节

**缺口表格示例**：
> **P0 缺口（必须补充）**：
> | 小节 | 缺失项 | 需澄清内容 |
> |------|--------|-----------|
> | 4.1 | 核心用户基础属性 | 请提供目标用户群体信息 |

**消息块 4（确认提示语）**：
> 📄 以上是 Basic + Core 阶段的完整分析文档。如需调整任何内容，请直接告知。

---

## 缺口讨论环节（如触发）

**性质：多轮对话（人在环中）**

缺口讨论不是一次性的问答，而是多轮人机对话过程：

- AI 在分析过程中主动发现问题（逻辑疑点、信息矛盾、P0 缺口）
- AI 主动发起提问，深入挖掘，而非被动等待用户触发
- 每轮对话后判断：缺口是否已补完？是否还有疑点需澄清？

**对话流程**：

```
发现缺口/疑点 → 呈现并提问 → 用户回应 → 
判断是否已补完 → 
  ├─ 已补完：输出确认，进入下一 Stage
  └─ 未补完：继续下一轮提问（循环）
```

**对话规范**：
- 每次只问 1-2 个问题（启用启发式追问，见 `references/collaboration-protocol.md`）
- 使用 HMW 思维提供替代方案（见 `references/analysis-methods.md`）
- 遇用户反驳，执行 5 步反驳机制（见 `references/collaboration-protocol.md`）
- 记录变更到 change-log.md

---

## 缺口补完确认

- 输出：`✓ 缺口已补完，进入 Stage 2`
- 不重新输出报告本体
- 更新版本迭代记录

---

## 输出文件更新

- 更新 `final-analysis.md` 一~四章节内容
- 版本迭代记录表新增条目（V1.0）
- 创建 `change-log.md`（记录协作过程）