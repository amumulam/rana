# Rana v0.3.2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Rana 从"资深体验设计师"升级为"体验架构师"，引入迁移成本评估、HMW 方案发散引擎、MVP 敏捷裁量三大能力。

**Architecture:** 修改 SKILL.md 的 6 个位置：顶部人设、Stage 1 诊断、Stage 2 Step 2.2 对话脚本、Stage 2 5步反驳机制、Stage 3 第六章模板、底部引用文件。纯 prompt 修改，无代码变更。

**Tech Stack:** Markdown 文档编辑，无依赖变更

---

## File Structure

| File | Operation | Responsibility |
|------|-----------|----------------|
| `rana/SKILL.md` | Modify | 6 处修改，注入 v0.3.2 能力 |

---

## Task 1: 更新顶部 version 和 description

**Files:**
- Modify: `rana/SKILL.md:1-31`

- [ ] **Step 1: 修改 version 字段**

将 `version: 0.3.1` 替换为 `version: 0.3.2`

- [ ] **Step 2: 修改 description 段落**

在现有 description 中，将 `【v0.3.1 核心升级】：引入咨询式批判思维...` 这一段后面追加 v0.3.2 升级标签：

```
    【v0.3.2 核心升级】：引入商业价值评估与敏捷落地思维。新增新旧方案迁移成本评估、HMW替代方案发散、以及 MVP 最小体验闭环定义，防止产品过度设计，保障核心价值快速上线。
```

实际做法：在现有 description 中 `【v0.3.1 核心升级】` 整行之后，用空行分隔添加 `【v0.3.2 核心升级】` 行。

- [ ] **Step 3: 修改正文第二行人设补充指令**

找到第 31 行（`帮助交互设计师将 PM 输入转化为设计可直接使用的结构化需求分析文档。你不仅是信息整理者...`），在这一行之后追加新的一行：

```markdown
你要时刻关注"投入产出比"与"用户迁移成本"。在方案遇到阻力时，不要陷入细节修补，要主动运用 HMW 思维提出更好的替代方向；在排优先级时，要敢于做减法，框定 MVP。
```

---

## Task 2: Stage 1 新增迁移成本自检诊断项

**Files:**
- Modify: `rana/SKILL.md`（查找 `### 逻辑合理性诊断（本次升级）` 章节）

- [ ] **Step 1: 在逻辑合理性诊断中新增第 3 条**

在现有的 2 条诊断项之后，增加第 3 条。找到 `2. **目标冲突检查**：...` 之后的位置，在其后的 `**处理逻辑：**` 之前插入：

```markdown
3. **迁移成本自检**：对比用户当前解决此问题的"旧方案"（或竞品方案），本次"新方案"是否给用户带来了过高的迁移成本（时间/金钱/认知负荷/安全顾虑）？
   - 若感知效用增量不足以覆盖用户的迁移成本，意味着用户大概率不会买单，将此列入 P0 探讨项
```

同时在 `**处理逻辑：**` 中，将现有内容后追加：

```markdown
- 若发现迁移成本过高 → 标记为 P0 级风险，在 Stage 2 触发 HMW 探讨
```

---

## Task 3: Stage 2 Step 2.2 引入 HMW 深度探讨

**Files:**
- Modify: `rana/SKILL.md`（查找 `**Step 2.2 — 逐项澄清` 章节）

- [ ] **Step 1: 修改 Step 2.2 标题和内容**

将：
```markdown
**Step 2.2 — 逐项澄清 P0 缺口**

按 P0 缺口清单顺序，逐项澄清：

> **问题 1（一、前置基础）**：请确认本次迭代版本号。
>
> _等待回答后记录_
```

替换为：

```markdown
**Step 2.2 — 逐项澄清与 HMW 深度探讨**

每次只问 1-2 个问题。针对 P0 缺口按清单顺序逐项澄清。

当发现 PM 的原方案存在高迁移成本、逻辑错位或方案-痛点明显脱节时，**强制触发 HMW (How Might We) 思维**：

- ❌ **不要仅对原方案"挑刺"**，陷入"这里不好、那里不好"的循环
- ✅ **必须主动提供 1-2 个完全不同方向的替代方案**（降维、绕道、更优体验）

> **HMW 示例**："既然目标是降低客服压力，除了当前的【增加负反馈表单】方案，我们是否可以 HMW：在用户搜索时直接前置展示【常见问题气泡】？"

常规缺口确认：
> **问题 1（一、前置基础）**：请确认本次迭代版本号。
>
> _等待回答后记录_
```

---

## Task 4: Stage 2 5 步反驳机制注入 HMW 发散

**Files:**
- Modify: `rana/SKILL.md`（查找 `### ⭐ 用户反驳处理规则（5 步机制）` 章节）

- [ ] **Step 1: 修改第 3 步深入探讨为 HMW 发散**

将第 3 步：
```markdown
3. **深入探讨**：提出反问引导思考："在极端的 [某具体] 场景下，你的方案如何应对？"
```

替换为：

```markdown
3. **深入探讨 (HMW 发散)**："如果这个方案在 [某极端场景] 下成本过高，我们能不能换个思路？比如，HMW [提出一个替代方向，如：在用户最需要时前置提供解决方案]？"
```

同时在第 4 步对比分析中，确保提到的是"替代方案"而非"你的建议"。将：
```markdown
4. **对比分析**："我们对比一下两个方向：方案A（你的）优势是...风险是...；方案B（我的建议）优势是...风险是..."
```

替换为：

```markdown
4. **对比分析**："我们对比一下两个方向：方案A（你的）优势是...风险是...；方案B（HMW 替代方案）优势是...风险是..."
```

---

## Task 5: Stage 3 第六章新增 MVP 最小体验闭环

**Files:**
- Modify: `rana/SKILL.md`（查找 `**六、需求清单与优先级排序**` 章节）

- [ ] **Step 1: 在第六章开头插入 MVP 特别指令**

在 `**六、需求清单与优先级排序**` 的填写说明之后、`### 6.1 需求排序模型` 之前插入：

```markdown
> 📝 **特别注意：MVP 最小体验闭环战略**。在列出需求表格前，AI 必须基于"资源有限"的假设，主动提炼一段 MVP 战略分析。明确向团队指出：哪些是保障核心价值跑通的最少功能集？哪些看似美好但耗费开发资源的边缘场景应当被果断砍掉或延期？

### 6.1 MVP 最小体验闭环定义

**MVP 核心主干**（保障核心价值跑通的最少功能集）：
| 需求ID | 需求简述 | 对应核心问题 |
|---|---|---|
| [需求ID] | [简要描述] | [对应问题] |

**建议砍掉/延期的场景**（不阻塞本次上线）：
| 需求/场景 | 建议 | 理由 |
|---|---|---|
| [需求描述] | 延期至下期 | [说明理由] |

### 6.2 需求全清单与优先级分级
```

- [ ] **Step 2: 重编号后续小节**

将原有的 `### 6.1 需求排序模型` 改为 `### 6.3 需求排序模型`

将原有的 `### 6.2 需求全清单与优先级分级` 改为 `### 6.4 需求全清单与优先级分级`

（注意：需要在 Stage 3 输出模板和输出目录约定中都检查是否引用了 6.1-6.3，同步更新）

---

## Task 6: 更新引用文件列表和纲要

**Files:**
- Modify: `rana/SKILL.md` 底部引用文件
- Modify: `rana/SKILL.md` 输出目录约定中的纲要

- [ ] **Step 1: 修改纲要中的第六章说明**

找到 `## 输出模板结构（final-analysis.md 纲要）` 中的：
```markdown
- 六、需求清单与优先级排序（关联至核心痛点）
```

替换为：
```markdown
- 六、需求清单与优先级排序（含 MVP 最小体验闭环定义、分层需求清单）
```

- [ ] **Step 2: 更新引用文件列表**

将：
```markdown
- `references/ux-analysis-methods.md` — 五问法/X-Y Problem/场景还原方法（Stage 1-2 使用）
```

替换为：

```markdown
- `references/ux-analysis-methods.md` — 五问法/HMW(How Might We)发散法/MVP敏捷拆解法/X-Y Problem/场景还原方法（Stage 1-2 使用）
```

---

## Verification

- [ ] SKILL.md version 字段为 0.3.2
- [ ] description 包含【v0.3.2 核心升级】标签
- [ ] 正文第二行有人设补充指令（投入产出比、迁移成本、HMW、做减法）
- [ ] Stage 1 逻辑合理性诊断有 3 条：方案-痛点匹配、目标冲突、迁移成本自检
- [ ] Stage 2 Step 2.2 标题为"逐项澄清与 HMW 深度探讨"，包含 HMW 强制触发指令
- [ ] Stage 2 5 步机制第 3 步改为 HMW 发散话术，第 4 步提到"替代方案"
- [ ] Stage 3 第六章有 6.1 MVP 最小体验闭环定义小节
- [ ] 引用文件列表包含 HMW/MVP 方法定义
- [ ] 运行 `python3 -m pytest tests/ -v` 全部通过

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-04-14-rana-0.3.2.md`.

**1. Subagent-Driven (recommended)** - 逐任务派发 subagent，任务间 review

**2. Inline Execution** - 本 session 直接执行

Which approach?
