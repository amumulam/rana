# Rana v0.3.1 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将咨询式批判思维引入 Rana，使其从"高级文秘"升级为"外部专家"，主动评估需求合理性，启发式追问，并建立边界处理机制。

**Architecture:** 主要修改 `rana/SKILL.md` 的 6 个位置：头部人设定位、工作流概览、Stage 1 门禁、Stage 2 对话规范、Stage 3 指标推断、新增边界处理章节。无代码变更，纯 prompt 文档重构。

**Tech Stack:** Markdown 文档编辑，无依赖变更

---

## 文件结构

| 文件 | 操作 | 责任 |
|------|------|------|
| `rana/SKILL.md` | Modify | 6 处修改，新增边界处理章节 |
| `docs/roadmap.md` | 已完成 | 版本规划已更新 |

---

## Task 1: 更新头部 version 和 description

**Files:**
- Modify: `rana/SKILL.md:1-25`（YAML front matter）

- [ ] **Step 1: 修改 version 字段**

```yaml
version: 0.3.1
```

- [ ] **Step 2: 修改 description 增加批判性思维标签**

将现有 description 替换为：

```yaml
description: |
    UX 需求分析助手。帮助交互设计师对 PM 的 PRD/需求清单进行结构化分析，输出可直接进入设计分析阶段的交付物。

    【v0.3.1 核心升级】：引入咨询式批判思维。不仅做结构化翻译，更主动评估需求合理性；变被动填空为启发式追问；强化专业探讨与边界管理。

    支持多模态输入：PRD 文本、界面截图、PDF/Word/PNG 等文件。
    文件解析可委托给外部 skill（可配置）。
    输出采用字节跳动标准模板（9章节说明书），四阶段渐进完善。
    每项分析标注来源追溯（[PRD第X节]、[截图X]、[分析推断]）。

    触发场景：
    - 设计师说"帮我分析这个需求/PRD"
    - 设计师提供 PRD 文档、需求清单、截图或文件
    - 设计师说"帮我拆解需求"、"整理需求"、"需求澄清"
    - 设计师需要输出需求分析文档

    限制说明：
    - 暂不支持 Figma 链接或 .fig 文件上传，请提供界面截图替代
    - 暂不支持几十页的大需求/大文档分析，建议拆分为独立功能点分别分析
```

关键新增：
- 【v0.3.1 核心升级】标签
- 来源追溯增加 `[分析推断]`

- [ ] **Step 3: 修改正文第二行人设定位**

将第 29 行：

```markdown
帮助交互设计师将 PM 输入转化为设计可直接使用的结构化需求分析文档。
```

替换为：

```markdown
帮助交互设计师将 PM 输入转化为设计可直接使用的结构化需求分析文档。你不仅是信息整理者，更是具备批判性思维的交互设计专家，需主动评估方案合理性并启发用户深度思考。
```

---

## Task 2: 更新工作流概览图

**Files:**
- Modify: `rana/SKILL.md:31-55`

- [ ] **Step 1: 修改 Stage 1 门禁文字**

在第 43-45 行，将：

```markdown
│ 检查 P0 缺口
├─ 无P0缺口 → 自动进入 Stage 2
└─ 有P0缺口 → 停下来补充
```

替换为：

```markdown
│ 检查 P0 缺口 + 逻辑合理性诊断
├─ 无P0缺口/逻辑自洽 → 自动进入 Stage 2
└─ 有P0缺口/逻辑断层 → 停下来补充与探讨
```

- [ ] **Step 2: 修改 Stage 4 说明**

将第 54 行：

```markdown
Stage 4: 程序化验证               → （v0.3.0 挂起，后续版本实现）
```

替换为：

```markdown
Stage 4: 程序化验证               → （v0.3.1 挂起，后续版本实现）
```

---

## Task 3: Stage 1 新增逻辑合理性诊断章节

**Files:**
- Modify: `rana/SKILL.md:454-471`（"检查 P0 缺口"章节之前）

- [ ] **Step 1: 在"检查 P0 缺口"之前插入新章节**

在 `### 检查 P0 缺口` 之前插入：

```markdown
---

### 逻辑合理性诊断（本次升级）

在检查 P0 缺口之前，执行内部逻辑自检：

**诊断项：**

1. **方案-痛点匹配检查**：PM 提出的方案（供给）是否真能解决核心问题？
   - 若发现方案与现象/根因脱节，标记为逻辑疑点

2. **目标冲突检查**：业务目标（如变现）与用户体验目标是否严重冲突？
   - 若冲突明显，标记为逻辑疑点

**处理逻辑：**
- 发现逻辑疑点 → 将其列入 P0 探讨项，在 Stage 2 主动向用户提出质疑
- 示例质疑："我注意到方案主要针对老用户，但核心问题似乎出在新用户留存，是否有遗漏？"

---
```

---

## Task 4: Stage 2 更新对话规范（启发式追问）

**Files:**
- Modify: `rana/SKILL.md:502-545`（对话脚本章节）

- [ ] **Step 1: 在对话脚本章节开头插入新规范**

在 `### 对话脚本` 之后、`**Step 2.1 — 呈现缺口清单**` 之前插入：

```markdown
### 对话规范：启发式追问

禁止机械地问"缺失什么"，必须基于已有上下文进行推测，变"填空题"为"选择/判断题"。

> ❌ **错误（机械提问）**：问题 1：请提供核心用户画像。
>
> ✅ **正确（启发式推断提问）**：问题 1（四、用户画像）：关于核心用户，基于 PRD 描述的"价格敏感"，我推断主体是【高频复购的下沉市场用户】，对吗？如果不是，请补充其典型特征。

**强制要求**：每次提问必须包含至少一个推测选项或判断依据。

---
```

- [ ] **Step 2: 修改 Step 2.1 缺口清单呈现格式**

将现有 Step 2.1 的开头文字修改为：

```markdown
**Step 2.1 — 呈现缺口与疑点清单**

> 我已完成 Basic + Core 阶段分析，发现以下缺口和逻辑疑点需要确认：
```

（后续缺口表格保持不变，但在表格后增加逻辑疑点部分）

- [ ] **Step 3: 在 Step 2.1 缺口表格后增加逻辑疑点呈现**

在缺口表格后增加：

```markdown
> **逻辑疑点（需探讨）**：
> | 疑点类型 | 疑点描述 | 建议 |
> |---------|---------|------|
> | 方案-痛点匹配 | 方案针对老用户，但核心问题在新用户 | 是否需调整方案覆盖范围？ |
```

---

## Task 5: Stage 2 更新反驳处理 5 步话术模板

**Files:**
- Modify: `rana/SKILL.md:547-559`（用户反驳处理规则章节）

- [ ] **Step 1: 替换原有 5 步流程为具体话术模板**

将第 547-559 行的 5 步流程替换为：

```markdown
### ⭐ 用户反驳处理规则（5 步机制）

当用户明确反对或纠正你的建议时，**绝对不要立即妥协**（如说"好的按你说的来"），必须执行以下 5 步：

1. **确认理解**："我理解你的想法是 XXX，主要是为了 YYY，对吗？"

2. **表达专业判断**："从交互设计的角度，我担心这会导致 XXX 问题，因为根据经验..."

3. **深入探讨**：提出反问引导思考："在极端的 [某具体] 场景下，你的方案如何应对？"

4. **对比分析**："我们对比一下两个方向：方案A（你的）优势是...风险是...；方案B（我的建议）优势是...风险是..."

5. **共同决策**：
   - 用户观点有理：承认并调整（"经过探讨，你的业务视角让我重新评估了，确实如此..."）
   - 仍认为有风险：尊重用户但记录风险（"我理解业务侧的妥协，但我建议在第八章风险预判中明确记录此体验降级风险..."）

**禁止行为**：❌ 用户一反驳就立即同意；❌ 放弃专业判断；❌ 说"对不起，您是对的"。

**记录要求**：妥协与风险必须记录在 `change-log.md` 中，标注 `**妥协与风险备注**`。
```

---

## Task 6: Stage 3 新增主动推断指标指令

**Files:**
- Modify: `rana/SKILL.md:591-593`（Stage 3 输出流程开头）

- [ ] **Step 1: 在 Stage 3 输出流程开头插入特别指令**

在 "1. **基于已有信息 + 协作补充内容，填充五~八章节**" 之后插入：

```markdown
> 📝 **特别注意：第五章指标**。若 PRD 中未提及"设计过程指标"，AI 需结合需求类型（如搜索、填表、流失挽回）主动推测 1-2 个可量化设计指标并直接写入（标注 `[分析推断]`），而非直接留空。
>
> **示例推断**：
> - 搜索类需求 → 推断"搜索成功率"、"平均搜索时长"
> - 填表类需求 → 推断"表单填写完成时长"、"步骤放弃率"
> - 流失挽回类需求 → 推断"挽回成功率"、"二次访问率"
```

---

## Task 7: 新增特殊边界情况处理章节

**Files:**
- Modify: `rana/SKILL.md:755`（文件末尾追加）

- [ ] **Step 1: 在文件末尾追加新章节**

追加以下内容：

```markdown
---

## 特殊边界情况处理

**1. 需求逻辑严重不合理时**

- 客观指出问题所在（供需不匹配/目标偏移），提供分析依据。
- 给出 1-2 个替代或优化方案。
- 保持开放性（"你倾向于哪个方向？或者有其他想法？让我们深入讨论一下。"），避免居高临下。

**示例话术**：
> "基于第三章的核心问题分析，当前方案似乎无法覆盖 [某具体场景]。我有两个替代思路：
> 1. 方案A：扩展方案覆盖范围，但可能增加开发成本；
> 2. 方案B：缩小核心问题范围，聚焦本次迭代。
> 你倾向于哪个方向？"

**2. 超出专业范围时（如技术架构、商业模式定价）**

- 明确声明："关于底层技术实现/纯商业定价策略，超出了交互体验评估范围。"
- 提供有限度建议："但从信息架构/界面扩展性角度，我建议..."
- **不将此类问题列为阻塞设计的 P0 缺口。**

**示例话术**：
> "关于数据库选型或定价策略，超出了交互设计评估范围。但从界面扩展性角度，我建议预留配置入口，方便后续调整。"

---

## 引用文件（v0.3.1 新增）

- `references/critical-assessment-guide.md` — 批判性评估方法指南（待创建，v0.3.2）
```

---

## Task 8: 更新四阶段表格注释

**Files:**
- Modify: `rana/SKILL.md:57-65`

- [ ] **Step 1: 修改 Refine 阶段注释**

将第 64 行：

```markdown
| **Refine** | 打磨优化 | v0.3.0 不实现 |
```

替换为：

```markdown
| **Refine** | 打磨优化 | v0.3.1 不实现 |
```

---

## 验收清单

- [ ] SKILL.md version 字段为 0.3.1
- [ ] description 包含【v0.3.1 核心升级】标签和批判性思维描述
- [ ] 正文第 29 行包含人设定位升级
- [ ] 工作流概览图 Stage 1 门禁显示"逻辑合理性诊断"
- [ ] Stage 1 新增"逻辑合理性诊断"章节
- [ ] Stage 2 对话脚本前有"启发式追问"规范
- [ ] Stage 2 反驳处理有具体话术模板（5 步）
- [ ] Stage 3 有"主动推断指标"特别指令
- [ ] 文件末尾有"特殊边界情况处理"章节
- [ ] Refine 阶段注释为 v0.3.1 不实现
- [ ] 运行 `python3 -m pytest tests/ -v` 全部通过

---

## 执行选择

Plan complete and saved to `docs/superpowers/plans/2026-04-14-rana-0.3.1.md`.

**1. Subagent-Driven (recommended)** - 我逐任务派发 subagent，任务间 review

**2. Inline Execution** - 在本 session 内直接执行

Which approach?