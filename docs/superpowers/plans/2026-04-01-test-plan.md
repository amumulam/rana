# UX Requirements Analyzer 全面测试计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 quality-validator.py 建立完整的 pytest 单元测试和 E2E 场景，并配置 pyproject.toml 使测试可一键运行。

**Architecture:** 分层结构——tests/unit/ 覆盖核心函数逻辑，tests/e2e/ 通过 subprocess 调用 CLI 验证完整场景。validator 本身不修改，测试只调用其公开函数和 CLI 接口。

**Tech Stack:** Python 3.9+, pytest, ruff, subprocess (E2E), pathlib (fixture 构造)

---

## 文件结构

```
requirements-analysis/
├── tests/
│   ├── __init__.py
│   ├── unit/
│   │   ├── __init__.py
│   │   ├── test_traceability.py
│   │   ├── test_vague_terms.py
│   │   ├── test_card_fields.py
│   │   └── test_sections.py
│   └── e2e/
│       ├── __init__.py
│       └── test_scenarios.py
├── test-runs/
│   ├── test-a-normal/      (已有)
│   ├── test-b-edge/        (已有)
│   ├── test-c-multi-feature/  (Task 4 新建)
│   ├── test-d-conflict/       (Task 4 新建)
│   └── test-e-traceability-fail/  (Task 4 新建)
└── pyproject.toml          (Task 1 新建)
```

---

## Task 1: pyproject.toml + tests skeleton

**Files:**
- Create: `pyproject.toml`
- Create: `tests/__init__.py`
- Create: `tests/unit/__init__.py`
- Create: `tests/e2e/__init__.py`

- [ ] **Step 1: 创建 pyproject.toml**

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]
addopts = "-v --tb=short"

[tool.ruff]
line-length = 100
target-version = "py39"
select = ["E", "F", "W", "I"]
exclude = ["test-runs/", ".ruff_cache/", "tests/"]
```

- [ ] **Step 2: 创建空 __init__.py 文件**

```bash
mkdir -p tests/unit tests/e2e
touch tests/__init__.py tests/unit/__init__.py tests/e2e/__init__.py
```

- [ ] **Step 3: 验证 pytest 可以发现测试（目前 0 个）**

Run: `python -m pytest tests/ -v`
Expected: `no tests ran` 或 `0 passed` （不报错即可）

- [ ] **Step 4: 验证 ruff 可以 lint validator**

Run: `ruff check ux-requirements-analyzer/scripts/`
Expected: 无报错或仅有已知问题

- [ ] **Step 5: Commit**

```bash
git add pyproject.toml tests/
git commit -m "chore: add pyproject.toml and tests skeleton"
```

---

## Task 2: test_traceability.py

**Files:**
- Create: `tests/unit/test_traceability.py`

validator 导入方式：validator 在 `ux-requirements-analyzer/scripts/quality-validator.py`，需要用 sys.path 导入：

```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent.parent / "ux-requirements-analyzer" / "scripts"))
from quality_validator import has_traceability, is_table_separator, check_traceability_input_structured
```

注意：文件名是 `quality-validator.py`（连字符），import 时需要用 importlib 或 rename。实际使用 importlib：
```python
import importlib.util, sys
from pathlib import Path
spec = importlib.util.spec_from_file_location(
    "quality_validator",
    Path(__file__).parent.parent.parent / "ux-requirements-analyzer" / "scripts" / "quality-validator.py"
)
mod = importlib.util.module_from_spec(spec)
spec.loader.exec_module(mod)
has_traceability = mod.has_traceability
is_table_separator = mod.is_table_separator
check_traceability_input_structured = mod.check_traceability_input_structured
```

测试用例（8个）：

| 测试名 | 输入 | 断言 |
|--------|------|------|
| test_has_traceability_prd | `"用户可发送消息 [PRD第2节]"` | True |
| test_has_traceability_infer | `"[推断：参考同类产品]"` | True |
| test_has_traceability_plain | `"用户可发送消息"` | False |
| test_is_table_separator | `"\|---|---\|"` | True |
| test_traceability_all_annotated | 3行全部带注释的表格内容 | pass_rate==1.0, issues==[] |
| test_traceability_none_annotated | 3行全无注释的表格内容 | pass_rate==0.0 |
| test_traceability_boundary_75pct | 4行中3行有注释 | pass_rate==0.75, len(issues)==1 |
| test_traceability_skip_code_block | 代码块内的行 | checked==0 |

- [ ] **Step 1: 写 test_traceability.py**

```python
import importlib.util
from pathlib import Path

def _load_validator():
    p = Path(__file__).parent.parent.parent / "ux-requirements-analyzer" / "scripts" / "quality-validator.py"
    spec = importlib.util.spec_from_file_location("quality_validator", p)
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    return mod

_v = _load_validator()
has_traceability = _v.has_traceability
is_table_separator = _v.is_table_separator
check_traceability_input_structured = _v.check_traceability_input_structured


def test_has_traceability_prd():
    assert has_traceability("用户可发送消息 [PRD第2节]") is True


def test_has_traceability_infer():
    assert has_traceability("[推断：参考同类产品]") is True


def test_has_traceability_plain():
    assert has_traceability("用户可发送消息") is False


def test_is_table_separator():
    assert is_table_separator("|---|---|") is True


def test_traceability_all_annotated():
    content = "| 功能 | 说明 | 来源 |\n|---|---|---|\n| 登录 | 用户登录 | [PRD第1节] |\n| 注册 | 新用户注册 | [PRD第2节] |\n| 退出 | 用户退出 | [PRD第3节] |"
    result = check_traceability_input_structured(content)
    assert result["pass_rate"] == 1.0
    assert result["issues"] == []


def test_traceability_none_annotated():
    content = "| 功能 | 说明 |\n|---|---|\n| 登录 | 用户登录 |\n| 注册 | 新用户注册 |\n| 退出 | 用户退出 |"
    result = check_traceability_input_structured(content)
    assert result["pass_rate"] == 0.0


def test_traceability_boundary_75pct():
    # 4行：3行有注释，1行无注释 → pass_rate = 0.75 < 0.80
    content = "| 功能 | 说明 |\n|---|---|\n| 登录 | 用户登录 [PRD第1节] |\n| 注册 | 新用户注册 [PRD第2节] |\n| 退出 | 用户退出 [PRD第3节] |\n| 搜索 | 用户搜索商品 |"
    result = check_traceability_input_structured(content)
    assert result["pass_rate"] == 0.75
    assert len(result["issues"]) == 1


def test_traceability_skip_code_block():
    # 代码块内的行不应被计入 checked
    content = "```\n无注释的行\n```"
    result = check_traceability_input_structured(content)
    assert result["checked"] == 0
```

- [ ] **Step 2: 运行测试，验证全部通过**

Run: `python -m pytest tests/unit/test_traceability.py -v`
Expected: 8 passed

- [ ] **Step 3: Commit**

```bash
git add tests/unit/test_traceability.py
git commit -m "test: add unit tests for traceability functions"
```

---

## Task 3: test_vague_terms.py

**Files:**
- Create: `tests/unit/test_vague_terms.py`

导入方式同 Task 2（importlib）。
函数：`check_vague_terms(content: str) -> list`

测试用例（5个）：

| 测试名 | 输入 | 断言 |
|--------|------|------|
| test_vague_triggers_shidang | `"请适当调整字体大小"` | len(issues) == 1, issues[0]["term"] == "适当" |
| test_vague_triggers_tbd | `"状态字段 TBD"` | len(issues) == 1, issues[0]["term"] == "TBD" |
| test_vague_no_trigger_xiangguan | `"通过相关度算法排序"` | issues == [] |
| test_vague_skip_code_block | `` "```\nTBD\n```" `` | issues 中无 TBD（注意：check_vague_terms 不跳过代码块，这是已知行为，测试记录实际行为） |
| test_vague_clean_text | `"用户点击按钮后进入详情页"` | issues == [] |

注意：check_vague_terms 当前实现不跳过代码块。`test_vague_skip_code_block` 应记录实际行为——如果代码块内 TBD 被检测到，断言 len(issues) >= 1。

- [ ] **Step 1: 写 test_vague_terms.py**

```python
import importlib.util
from pathlib import Path

def _load_validator():
    p = Path(__file__).parent.parent.parent / "ux-requirements-analyzer" / "scripts" / "quality-validator.py"
    spec = importlib.util.spec_from_file_location("quality_validator", p)
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    return mod

_v = _load_validator()
check_vague_terms = _v.check_vague_terms


def test_vague_triggers_shidang():
    issues = check_vague_terms("请适当调整字体大小")
    assert len(issues) == 1
    assert issues[0]["term"] == "适当"


def test_vague_triggers_tbd():
    issues = check_vague_terms("状态字段 TBD")
    assert len(issues) == 1
    assert issues[0]["term"] == "TBD"


def test_vague_no_trigger_xiangguan():
    # "相关" 已从 VAGUE_TERMS 移除，不应触发
    issues = check_vague_terms("通过相关度算法排序")
    assert issues == []


def test_vague_code_block_behavior():
    # check_vague_terms 不跳过代码块——记录实际行为
    content = "```\nTBD\n```"
    issues = check_vague_terms(content)
    # 代码块内 TBD 会被检测到（已知行为）
    assert any(i["term"] == "TBD" for i in issues)


def test_vague_clean_text():
    issues = check_vague_terms("用户点击按钮后进入详情页，查看订单状态")
    assert issues == []
```

- [ ] **Step 2: 运行测试**

Run: `python -m pytest tests/unit/test_vague_terms.py -v`
Expected: 5 passed

- [ ] **Step 3: Commit**

```bash
git add tests/unit/test_vague_terms.py
git commit -m "test: add unit tests for vague terms check"
```

---

## Task 4: test_card_fields.py

**Files:**
- Create: `tests/unit/test_card_fields.py`

函数：`check_card_fields(content: str) -> dict`
返回：`{"missing": [...], "extra": [...], "found": [...], "status": "PASS"|"WARN"|"FAIL"}`

REQUIRED_CARD_FIELDS（14个，按顺序）：
需求名称、版本/迭代号、需求来源、背景说明、目标用户、使用场景、核心问题、需求拆解、业务规则、约束条件、优先级、待澄清项、风险点、需求结论

完整表格内容（用于 PASS 测试）：
```
## 交付物 A：需求分析卡

| 字段 | 内容 | 来源追溯 |
|------|------|---------|
| **需求名称** | 测试需求 | [PRD第1节] |
| **版本/迭代号** | v1.0 | [PRD第1节] |
| **需求来源** | 用户反馈 | [PRD第1节] |
| **背景说明** | 背景内容 | [PRD第1节] |
| **目标用户** | 已登录用户 | [PRD第1节] |
| **使用场景** | 日常使用 | [PRD第1节] |
| **核心问题** | 核心问题描述 | [PRD第1节] |
| **需求拆解** | [见交付物 B] | — |
| **业务规则** | 业务规则内容 | [PRD第1节] |
| **约束条件** | 约束内容 | [PRD第1节] |
| **优先级** | P0 | [PRD第1节] |
| **待澄清项** | [见交付物 D] | — |
| **风险点** | 风险内容 | [PRD第1节] |
| **需求结论** | [见交付物 E] | — |
```

测试用例（5个）：

- [ ] **Step 1: 写 test_card_fields.py**

```python
import importlib.util
from pathlib import Path

def _load_validator():
    p = Path(__file__).parent.parent.parent / "ux-requirements-analyzer" / "scripts" / "quality-validator.py"
    spec = importlib.util.spec_from_file_location("quality_validator", p)
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    return mod

_v = _load_validator()
check_card_fields = _v.check_card_fields

FULL_CARD = """## 交付物 A：需求分析卡

| 字段 | 内容 | 来源追溯 |
|------|------|---------|
| **需求名称** | 测试需求 | [PRD第1节] |
| **版本/迭代号** | v1.0 | [PRD第1节] |
| **需求来源** | 用户反馈 | [PRD第1节] |
| **背景说明** | 背景内容 | [PRD第1节] |
| **目标用户** | 已登录用户 | [PRD第1节] |
| **使用场景** | 日常使用 | [PRD第1节] |
| **核心问题** | 核心问题描述 | [PRD第1节] |
| **需求拆解** | [见交付物 B] | — |
| **业务规则** | 业务规则内容 | [PRD第1节] |
| **约束条件** | 约束内容 | [PRD第1节] |
| **优先级** | P0 | [PRD第1节] |
| **待澄清项** | [见交付物 D] | — |
| **风险点** | 风险内容 | [PRD第1节] |
| **需求结论** | [见交付物 E] | — |
"""


def test_card_fields_pass():
    result = check_card_fields(FULL_CARD)
    assert result["status"] == "PASS"
    assert result["missing"] == []
    assert result["extra"] == []


def test_card_fields_fail_missing_one():
    # 去掉「需求拆解」行
    content = FULL_CARD.replace("| **需求拆解** | [见交付物 B] | — |\n", "")
    result = check_card_fields(content)
    assert result["status"] == "FAIL"
    assert "需求拆解" in result["missing"]


def test_card_fields_warn_extra():
    # 加入 2 个额外字段
    extra_rows = "| **自定义字段A** | 额外内容 | [PRD第1节] |\n| **自定义字段B** | 额外内容 | [PRD第1节] |\n"
    content = FULL_CARD.rstrip() + "\n" + extra_rows
    result = check_card_fields(content)
    assert result["status"] == "WARN"
    assert len(result["extra"]) == 2


def test_card_fields_no_section():
    # 文档不含需求分析卡章节标题 → 视为 found=[] → FAIL（返回全部缺失）
    content = "# 随便一篇文档\n\n没有需求分析卡章节\n"
    result = check_card_fields(content)
    # 无章节时 found=[]，函数返回所有字段缺失的 FAIL
    assert result["status"] == "FAIL"
    assert len(result["missing"]) == 14


def test_card_fields_truncation_8_missing():
    # 缺失 8 个字段，missing 列表长度应为 8
    lines_to_remove = ["需求拆解", "业务规则", "约束条件", "优先级", "待澄清项", "风险点", "需求结论", "背景说明"]
    content = FULL_CARD
    for field in lines_to_remove:
        content = content.replace(f"| **{field}**", f"| **REMOVED_{field}**")
    result = check_card_fields(content)
    assert result["status"] == "FAIL"
    assert len(result["missing"]) == 8
```

- [ ] **Step 2: 运行测试**

Run: `python -m pytest tests/unit/test_card_fields.py -v`
Expected: 5 passed

- [ ] **Step 3: Commit**

```bash
git add tests/unit/test_card_fields.py
git commit -m "test: add unit tests for card field completeness check"
```

---

## Task 5: test_sections.py

**Files:**
- Create: `tests/unit/test_sections.py`

函数：
- `check_sections(content: str, expected_sections: list) -> dict` → `{"pass": [...], "fail": [...]}`
- `check_file_structure(analysis_dir: Path) -> dict` → `{"pass": [...], "fail": [...]}`

EXPECTED_SECTIONS for final-analysis.md：
`["需求分析卡", "需求拆解清单", "场景与边界说明", "待澄清问题清单", "需求分析结论"]`

- [ ] **Step 1: 写 test_sections.py**

```python
import importlib.util
from pathlib import Path

def _load_validator():
    p = Path(__file__).parent.parent.parent / "ux-requirements-analyzer" / "scripts" / "quality-validator.py"
    spec = importlib.util.spec_from_file_location("quality_validator", p)
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    return mod

_v = _load_validator()
check_sections = _v.check_sections
check_file_structure = _v.check_file_structure

EXPECTED = ["需求分析卡", "需求拆解清单", "场景与边界说明", "待澄清问题清单", "需求分析结论"]

FULL_FINAL = """## 需求分析卡
内容
## 需求拆解清单
内容
## 场景与边界说明
内容
## 待澄清问题清单
内容
## 需求分析结论
内容
"""


def test_sections_all_present():
    result = check_sections(FULL_FINAL, EXPECTED)
    assert result["fail"] == []
    assert len(result["pass"]) == 5


def test_sections_missing_one():
    content = FULL_FINAL.replace("## 需求分析结论\n内容", "")
    result = check_sections(content, EXPECTED)
    assert "需求分析结论" in result["fail"]


def test_file_structure_all_present(tmp_path):
    for fname in ["input-structured.md", "gap-analysis.md", "change-log.md",
                  "quality-report.md", "final-analysis.md"]:
        (tmp_path / fname).write_text("content")
    result = check_file_structure(tmp_path)
    assert result["fail"] == []
    assert len(result["pass"]) == 5


def test_file_structure_missing_one(tmp_path):
    for fname in ["input-structured.md", "change-log.md",
                  "quality-report.md", "final-analysis.md"]:
        (tmp_path / fname).write_text("content")
    # gap-analysis.md 故意不创建
    result = check_file_structure(tmp_path)
    assert "gap-analysis.md" in result["fail"]
```

- [ ] **Step 2: 运行测试**

Run: `python -m pytest tests/unit/test_sections.py -v`
Expected: 4 passed

- [ ] **Step 3: 运行全部单元测试确认通过**

Run: `python -m pytest tests/unit/ -v`
Expected: 22 passed（Task 2-5 累计）

- [ ] **Step 4: Commit**

```bash
git add tests/unit/test_sections.py
git commit -m "test: add unit tests for sections and file structure checks"
```

---

## Task 6: 创建 E2E fixture（test-c, test-d, test-e）

**Files:**
- Create: `test-runs/test-c-multi-feature/` (5 个文件)
- Create: `test-runs/test-d-conflict/` (5 个文件)
- Create: `test-runs/test-e-traceability-fail/` (5 个文件)

### test-c-multi-feature（多功能 PRD，预期 PASS）

`final-analysis.md` 包含两张需求分析卡（消息通知 + 消息搜索），各 14/14 字段。
其余 4 个文件结构正常，全部有来源注释。

### test-d-conflict（冲突解决路径，预期 PASS）

`change-log.md` 包含一条 `⚠ 待讨论` 状态条目（PM 与研发对消息上限有分歧）。
其余文件正常。验证器当前跳过 change-log，预期 PASS。

### test-e-traceability-fail（来源追溯失败，预期 FAIL）

`input-structured.md` 中 10 行数据行只有 6 行带注释（注释率 60% < 80%）。
其余文件正常，`final-analysis.md` 有 14/14 字段。

- [ ] **Step 1: 创建 test-c-multi-feature 的 5 个文件**

`input-structured.md`（全部有注释）、`gap-analysis.md`（全部有注释）、`change-log.md`（正常格式）、`quality-report.md`（正常格式）。

`final-analysis.md` 需包含两张卡，格式如下（关键）：

```markdown
## 交付物 A：需求分析卡

### 消息通知功能

| 字段 | 内容 | 来源追溯 |
|------|------|---------|
| **需求名称** | 消息通知 | [PRD第1节] |
...（14个字段）

### 消息搜索功能

| 字段 | 内容 | 来源追溯 |
|------|------|---------|
| **需求名称** | 消息搜索 | [PRD第2节] |
...（14个字段）
```

注意：check_card_fields 检测到第一张卡后在下一个 `## ` 标题处停止。因此两张卡都在同一个 `## 交付物 A` 节内，用 `### ` 子标题区分。

- [ ] **Step 2: 创建 test-d-conflict 的 5 个文件**

`change-log.md` 关键内容：
```markdown
### CHG-001：消息上限——⚠ 待讨论
- **时间**：2026-04-01
- **触发**：PM 提议上限 100 条，研发认为应为 200 条
- **确认方**：⚠ 待讨论
- **内容**：消息上限数量存在分歧，PM 主张 100 条，研发主张 200 条，待本周会议确认
- **影响**：阻塞需求拆解维度业务规则定义
```

其余文件与 test-a-normal 格式相同，`final-analysis.md` 有 14/14 字段。

- [ ] **Step 3: 创建 test-e-traceability-fail 的 5 个文件**

`input-structured.md` 必须有 10 行数据行，其中 4 行无来源注释（实际注释率 = 6/10 = 60%）：

```markdown
| # | 功能点描述 | 来源 |
|---|-----------|------|
| 1 | 新增搜索页面 | [PRD第1节] |
| 2 | 支持关键词搜索 | [PRD第1节] |
| 3 | 搜索结果列表展示 | [PRD第2节] |
| 4 | 搜索历史记录 | [PRD第2节] |
| 5 | 热门搜索词展示 | [PRD第3节] |
| 6 | 搜索结果排序 | [PRD第3节] |
| 7 | 无结果状态展示 |  |
| 8 | 搜索加载状态 |  |
| 9 | 搜索词高亮 |  |
| 10 | 搜索过滤条件 |  |
```

注意：`check_traceability_input_structured` 会跳过表头行（`#` 关键词），只计数数据行。第 7-10 行无来源注释，pass_rate = 6/10 = 60% < 80%。

其余 4 个文件正常（gap-analysis.md, change-log.md, quality-report.md, final-analysis.md 各有来源注释，final-analysis.md 有 14/14 字段）。

- [ ] **Step 4: 手动运行验证器确认三个场景的预期结果**

```bash
python3 ux-requirements-analyzer/scripts/quality-validator.py test-runs/test-c-multi-feature
# 预期：✓ PASS，exit 0

python3 ux-requirements-analyzer/scripts/quality-validator.py test-runs/test-d-conflict
# 预期：✓ PASS，exit 0

python3 ux-requirements-analyzer/scripts/quality-validator.py test-runs/test-e-traceability-fail
# 预期：✗ FAIL，exit 1，失败维度包含 input-structured.md 可追溯性
```

- [ ] **Step 5: Commit**

```bash
git add test-runs/test-c-multi-feature/ test-runs/test-d-conflict/ test-runs/test-e-traceability-fail/
git commit -m "test: add E2E test fixtures (multi-feature, conflict, traceability-fail)"
```

---

## Task 7: test_scenarios.py（E2E 测试）

**Files:**
- Create: `tests/e2e/test_scenarios.py`

通过 `subprocess.run` 调用 CLI，断言退出码和标准输出关键词。

| 场景 | 退出码 | 输出关键词 |
|------|--------|-----------|
| test-a-normal | 0 | `✓ PASS` |
| test-b-edge | 1 | `字段完整性` |
| test-c-multi-feature | 0 | `✓ PASS` |
| test-d-conflict | 0 | `✓ PASS` |
| test-e-traceability-fail | 1 | `来源追溯` |

- [ ] **Step 1: 写 test_scenarios.py**

```python
import subprocess
import sys
from pathlib import Path

REPO_ROOT = Path(__file__).parent.parent.parent
VALIDATOR = REPO_ROOT / "ux-requirements-analyzer" / "scripts" / "quality-validator.py"
TEST_RUNS = REPO_ROOT / "test-runs"


def run_validator(scenario_dir: Path):
    result = subprocess.run(
        [sys.executable, str(VALIDATOR), str(scenario_dir)],
        capture_output=True,
        text=True,
    )
    return result.returncode, result.stdout


def test_scenario_a_normal():
    code, out = run_validator(TEST_RUNS / "test-a-normal")
    assert code == 0, f"Expected exit 0, got {code}\nOutput:\n{out}"
    assert "✓ PASS" in out


def test_scenario_b_edge():
    code, out = run_validator(TEST_RUNS / "test-b-edge")
    assert code == 1, f"Expected exit 1, got {code}\nOutput:\n{out}"
    assert "字段完整性" in out


def test_scenario_c_multi_feature():
    code, out = run_validator(TEST_RUNS / "test-c-multi-feature")
    assert code == 0, f"Expected exit 0, got {code}\nOutput:\n{out}"
    assert "✓ PASS" in out


def test_scenario_d_conflict():
    code, out = run_validator(TEST_RUNS / "test-d-conflict")
    assert code == 0, f"Expected exit 0, got {code}\nOutput:\n{out}"
    assert "✓ PASS" in out


def test_scenario_e_traceability_fail():
    code, out = run_validator(TEST_RUNS / "test-e-traceability-fail")
    assert code == 1, f"Expected exit 1, got {code}\nOutput:\n{out}"
    assert "来源追溯" in out or "可追溯性" in out
```

- [ ] **Step 2: 运行 E2E 测试**

Run: `python -m pytest tests/e2e/test_scenarios.py -v`
Expected: 5 passed

- [ ] **Step 3: 运行全部测试**

Run: `python -m pytest tests/ -v`
Expected: 27 passed（22 unit + 5 e2e）

- [ ] **Step 4: Commit**

```bash
git add tests/e2e/test_scenarios.py
git commit -m "test: add E2E scenario tests"
```

---

## 完成后验证

运行以下命令确认全部通过：

```bash
python -m pytest tests/ -v
ruff check ux-requirements-analyzer/scripts/
```

预期输出：
- pytest: 27 passed
- ruff: no errors（或仅有预先存在的已知问题）
