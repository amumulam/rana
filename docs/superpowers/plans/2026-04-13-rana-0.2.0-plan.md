# Rana 0.2.0 实现计划 — 文件预处理委托机制

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 rana skill 新增 Stage 0 文件预处理能力，将 PDF/Word/PNG 等输入文件的解析委托给外部 skill。

**Architecture:** 在 SKILL.md 中新增 Stage 0 instructions，包括配置读取、skill 调用、状态轮询、fallback 等逻辑。创建 config.yaml 配置文件指定各文件类型的解析 skill。

**Tech Stack:** YAML 配置、SKILL.md instructions、sessions_spawn 子 agent 调用

---

## 文件结构

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `rana/config.yaml` | 新建 | 文件解析配置（指定各类型用哪个 skill） |
| `rana/SKILL.md` | 修改 | 版本升级 + 新增 Stage 0 章节 |
| `~/.openclaw/skills/rana/config.yaml` | 同步 | 安装时复制到 OpenClaw 目录 |

---

### Task 1: 创建 config.yaml 配置文件

**Files:**
- Create: `rana/config.yaml`

- [ ] **Step 1: 创建配置文件**

```yaml
# Rana 文件解析配置
# 指定各文件类型使用哪个 OpenClaw skill 解析
# 未配置的类型将使用 fallback（模型多模态解读）

file_parser:
  pdf: "mineru-pipeline"
  word: "mineru-pipeline"
  png: "mineru-pipeline"
  docx: "mineru-pipeline"
  # 用户可添加更多类型或替换为其他 skill
```

- [ ] **Step 2: 验证文件创建**

```bash
cat rana/config.yaml
```

- [ ] **Step 3: 同步到 OpenClaw 目录**

```bash
cp rana/config.yaml ~/.openclaw/skills/rana/config.yaml
```

---

### Task 2: 更新 SKILL.md 版本号

**Files:**
- Modify: `rana/SKILL.md:1-6`（frontmatter）

- [ ] **Step 1: 读取当前 frontmatter**

```bash
head -10 rana/SKILL.md
```

- [ ] **Step 2: 更新版本号**

将 `version: 0.1.0` 改为 `version: 0.2.0`

同时更新 `description` 中的能力描述：

```yaml
---
name: rana
version: 0.2.0
license: MIT
metadata:
  author: amumu
  description: |
    UX 需需分析助手。帮助交互设计师对 PM 的 PRD/需求清单进行结构化分析，输出可直接进入设计分析阶段的交付物。

    支持多模态输入：PRD 文本、界面截图、PDF/Word/PNG 等文件。
    文件解析可委托给外部 skill（可配置）。
    每项分析标注来源追溯（[PRD第X节]、[截图X]）。
    通过5维度质量门禁保障输出完整性。

    触发场景：
    - 设计师说"帮我分析这个需求/PRD"
    - 设计师提供 PRD 文档、需求清单、截图或文件
    - 设计师说"帮我拆解需求"、"整理需求"、"需求澄清"
    - 设计师需要输出需求分析文档

    限制说明：
    - 暂不支持 Figma 链接或 .fig 文件上传，请提供界面截图替代
    - 暂不支持几十页的大需求/大文档分析，建议拆分为独立功能点分别分析
---
```

- [ ] **Step 3: 同步到 OpenClaw 目录**

```bash
cp rana/SKILL.md ~/.openclaw/skills/rana/SKILL.md
```

---

### Task 3: 新增 Stage 0 章节

**Files:**
- Modify: `rana/SKILL.md`（在 Stage 1 之前插入）

- [ ] **Step 1: 找到 Stage 1 章节的位置**

当前 SKILL.md 的工作流概览在约第 32 行开始。需要在 Stage 1 之前插入 Stage 0。

- [ ] **Step 2: 更新工作流概览**

将工作流概览更新为：

```markdown
## 工作流概览

输入: 任意文件（PDF/Word/PNG/文字/Markdown）
（Figma 链接/文件暂不支持，引导用户提供界面截图替代）
       │
       ▼
Stage 0: 文件预处理（新增）     → 结构化 Markdown
       │
       ▼
Stage 1: 输入解析与结构化    → input-structured.md
       │
       ▼
Stage 2: 需求缺口分析与拆解  → gap-analysis.md
       │
       ▼
Stage 3: 协作对话补充        → change-log.md
       │
       ▼
Stage 4: 质量门禁验证（AI 自评） → quality-report.md
       │
       ▼ (PASS)
Stage 5: 整合输出            → final-analysis.md（含5项交付物）
       │
       ▼
Stage 6: 程序化验证          → 更新 quality-report.md
```

- [ ] **Step 3: 新增 Stage 0 详细说明章节**

在工作流概览后、Stage 1 之前，插入：

```markdown
---

## Stage 0: 文件预处理

**当用户输入为文件路径（而非纯文字内容）时，执行此阶段。**

### 触发条件

- 用户输入以文件路径形式提供（包含扩展名）
- 文件类型：PDF / Word / PNG / DOCX / 等

### 流程步骤

#### 步骤 1：检测文件类型

提取文件扩展名（如 `.pdf` → `pdf`）。

#### 步骤 2：读取配置文件

配置文件位置：`~/.openclaw/skills/rana/config.yaml`

```yaml
file_parser:
  pdf: "mineru-pipeline"
  word: "mineru-pipeline"
  ...
```

**配置逻辑**：
- 配置文件存在？查找 `file_parser.<类型>`
  - 找到？→ 使用指定 skill
  - 未找到？→ 执行 fallback
- 配置文件不存在？→ 执行 fallback

#### 步骤 3：调用外部 skill（sessions_spawn）

**参数准备**：
- `inputPath`: 用户文件路径
- `outputDir`: `ux-requirement-analysis/_temp/`

**子 agent 创建**：
```
sessions_spawn(
  task: "使用 {skill_name} skill 解析文件 {file_path}，输出到 {outputDir}",
  skills: ["{skill_name}"]
)
```

**状态轮询**：
- 每 60 秒检查子 agent 状态
- 正常进行 → 继续等待下一轮
- 完成 → 读取输出文件
- 异常 → 报告错误，提供用户选择

#### 步骤 4：读取解析结果

**输出文件位置**：
```
ux-requirement-analysis/_temp/{filename}/auto/{filename}.md
```

读取此 Markdown 文件内容，作为 Stage 1 的输入。

#### 步骤 5：自动衔接 Stage 1

无需用户确认，直接进入 Stage 1 流程。

### Fallback 机制

**触发条件**：
- `config.yaml` 不存在
- 文件类型未配置

**Fallback 流程**：
1. 告知用户："当前文件类型未配置解析器，正在使用模型多模态能力直接解读"
2. 调用模型多模态能力读取文件
3. 输出文字描述作为临时 Markdown 内容
4. 自动衔接 Stage 1

### 错误处理

| 错误 | 处理 |
|------|------|
| skill 不存在 | 提示用户安装或修改配置 + 执行 fallback |
| 解析失败 | 提供选择：重试 / fallback / 手动输入 |
| 输出文件缺失 | 执行 fallback |

**用户选择提示格式**：
```
解析失败。请选择：
1. 重试（再次调用 skill）
2. 使用 fallback（模型多模态解读）
3. 手动输入（您提供文字内容）
```

---

## Stage 1: 输入解析与结构化

（后续内容保持不变）
```

- [ ] **Step 4: 同步到 OpenClaw 目录**

```bash
cp rana/SKILL.md ~/.openclaw/skills/rana/SKILL.md
```

---

### Task 4: 测试验证

- [ ] **Step 1: 验证配置文件**

```bash
cat ~/.openclaw/skills/rana/config.yaml
```

Expected: 显示 YAML 配置内容

- [ ] **Step 2: 验证 SKILL.md 包含 Stage 0**

```bash
grep -n "Stage 0" ~/.openclaw/skills/rana/SKILL.md
```

Expected: 找到 Stage 0 章节

- [ ] **Step 3: 手动测试（在 OpenClaw 中）**

输入一个 PDF 文件路径，观察：
- 是否触发 Stage 0
- 是否调用 mineru-pipeline
- 是否自动衔接 Stage 1

---

### Task 5: 提交改动

- [ ] **Step 1: 检查改动文件**

```bash
git status
```

Expected: `rana/config.yaml`（新建）、`rana/SKILL.md`（修改）

- [ ] **Step 2: 提交**

```bash
git add rana/config.yaml rana/SKILL.md
git commit -m "feat: add Stage 0 file preprocessing for rana 0.2.0

- Create config.yaml for file parser skill mapping
- Update SKILL.md version to 0.2.0
- Add Stage 0 instructions: file type detection, skill delegation, fallback
- Support PDF/Word/PNG via mineru-pipeline (configurable)"
```

---

## 验收标准

| # | 验收项 | 验证方式 |
|---|--------|---------|
| 1 | config.yaml 存在且格式正确 | `cat rana/config.yaml` |
| 2 | SKILL.md 版本 0.2.0 | `grep "version: 0.2.0" rana/SKILL.md` |
| 3 | Stage 0 章节存在 | `grep "Stage 0" rana/SKILL.md` |
| 4 | 同步到 OpenClaw | `ls ~/.openclaw/skills/rana/config.yaml` |
| 5 | 手动测试（可选） | 在 OpenClaw 中输入 PDF 文件 |

---

## Spec 自检

| 检查项 | 结果 |
|--------|------|
| Spec 覆盖 | ✅ Task 1-3 覆盖配置 + SKILL.md 改动 |
| Placeholder | ✅ 无 TBD/TODO |
| 一致性 | ✅ config.yaml 结构与 SKILL.md 说明一致 |