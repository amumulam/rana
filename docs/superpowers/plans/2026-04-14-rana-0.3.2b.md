# Rana v0.3.2b Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v0.3.2 微调版本：将迭代版本号从 P0 降级为 P1，不再阻塞流程；各 Stage 完成后全量展示 final-analysis。

**Architecture:** 修改 SKILL.md 的 3 个位置：Stage 1 P0 缺口清单（移除版本号）、Stage 1 输出末尾新增展示指令、Stage 2 输出末尾新增展示指令。纯 prompt 修改。

**Tech Stack:** Markdown 文档编辑，无依赖变更

---

## File Structure

| File | Operation | Responsibility |
|------|-----------|----------------|
| `rana/SKILL.md` | Modify | 3 处修改 |

---

## Task 1: 更新顶部 version

**Files:**
- Modify: `rana/SKILL.md:3`

- [ ] **Step 1: 修改 version 字段**

将 `version: 0.3.2` 替换为 `version: 0.3.2b`

---

## Task 2: Stage 1 P0 缺口清单移除版本号

**Files:**
- Modify: `rana/SKILL.md`（查找 `### 检查 P0 缺口` 章节）

- [ ] **Step 1: 修改 P0 缺口清单**

找到 `### 检查 P0 缺口` 章节中的 P0 缺口列表：

将：
```markdown
**Basic 阶段 P0 缺口：**
- 一、1.1 文档基础信息：需求名称、业务线、版本号
- 二、2.1 需求来源：至少一项溯源依据
- 二、2.3 影响范围：覆盖用户群体
```

替换为：
```markdown
**Basic 阶段 P0 缺口：**
- 一、1.1 文档基础信息：需求名称、业务线
- 二、2.1 需求来源：至少一项溯源依据
- 二、2.3 影响范围：覆盖用户群体

**Basic 阶段 P1 缺口（可带病推进）：**
- 一、1.1 文档基础信息：迭代版本号（可后续补充）
```

---

## Task 3: Stage 1 输出末尾新增全量展示指令

**Files:**
- Modify: `rana/SKILL.md`（查找 Stage 1 输出区域）

- [ ] **Step 1: 在 Stage 1 输出流程末尾新增展示指令**

找到 Stage 1 的输出流程最后（在进入 Stage 2 之前），在 `---` 分隔线之前插入：

```markdown
**Stage 1 输出完成后，必须向用户呈现当前 final-analysis.md 全量内容：**

> 以下是目前的需求分析文档（一~四章节），请确认内容是否准确。

[输出 final-analysis.md 一~四章节完整内容]

> 如需调整任何内容，请直接告知。确认无误后，我们将进入 Stage 2 协作补充。

---
```

---

## Task 4: Stage 2 输出末尾新增全量展示指令

**Files:**
- Modify: `rana/SKILL.md`（查找 Stage 2 输出区域）

- [ ] **Step 1: 在 Stage 2 输出流程末尾新增展示指令**

找到 Stage 2 的 `### 输出` 章节，在 `---` 分隔线之前插入：

```markdown
**Stage 2 协作完成后，必须向用户呈现当前 final-analysis.md 全量内容：**

> 以下是更新后的需求分析文档（一~四章节），请确认补充内容是否准确。

[输出 final-analysis.md 一~四章节完整内容]

> 如需调整任何内容，请直接告知。确认无误后，我们将进入 Stage 3 Detail 输出。

---
```

---

## Verification

- [ ] SKILL.md version 字段为 0.3.2b
- [ ] Stage 1 P0 缺口清单不包含「版本号」
- [ ] Stage 1 P1 缺口包含「迭代版本号」
- [ ] Stage 1 输出末尾有「全量展示 final-analysis」指令
- [ ] Stage 2 输出末尾有「全量展示 final-analysis」指令
- [ ] 运行 `python3 -m pytest tests/ -v` 全部通过

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-04-14-rana-0.3.2b.md`.

**1. Subagent-Driven (recommended)** - 逐任务派发，任务间 review

**2. Inline Execution** - 本 session 直接执行

Which approach?